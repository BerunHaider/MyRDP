name: "⚡ Karpux Labs | Ultimate Mobile DevEnv"

on:
  workflow_dispatch:

jobs:
  deploy-ultimate-box:
    name: "📱 DevBox: ${{ github.run_id }}"
    runs-on: windows-latest
    timeout-minutes: 360 # 6 Horas Máximo

    defaults:
      run:
        shell: pwsh

    env:
      TS_AUTHKEY: ${{ secrets.TAILSCALE_AUTHKEY }}
      RCLONE_CONF_CONTENT: ${{ secrets.RCLONE_CONF }}
      STATIC_PASS: ${{ secrets.VM_PASSWORD }}
      DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
      HOSTNAME: "karpux-dev-box"
      NODE_OPTIONS: "--max_old_space_size=4096"

    steps:
      # 1. Notificación de Inicio
      - name: 🔔 Discord - Iniciando
        if: env.DISCORD_WEBHOOK != ''
        run: |
          $payload = @{ content = "🟡 **Karpux Labs** | Iniciando despliegue de infraestructura..." }
          Invoke-RestMethod -Uri $env:DISCORD_WEBHOOK -Method Post -Body ($payload | ConvertTo-Json) -ContentType 'application/json'

      # 2. Configuración de Usuario y Red (VERSIÓN POWERSHELL NATIVO)
      - name: 🛠️ Preparar Sistema
        run: |
          Write-Host "🔹 Configurando Admin (Método Moderno)..."
          $user = "runneradmin"
          $securePass = ConvertTo-SecureString $env:STATIC_PASS -AsPlainText -Force

          if (-not (Get-LocalUser -Name $user -ErrorAction SilentlyContinue)) {
              New-LocalUser -Name $user -Password $securePass -Description "Karpux Dev Admin" -PasswordNeverExpires | Out-Null
          } else {
              Set-LocalUser -Name $user -Password $securePass
          }

          Add-LocalGroupMember -Group "Administrators" -Member $user -ErrorAction SilentlyContinue
          
          Write-Host "🔹 Abriendo Firewall..."
          try {
              Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -value 0 -ErrorAction Stop
              Enable-NetFirewallRule -DisplayGroup "Remote Desktop" -ErrorAction Stop
          } catch {
              Write-Host "⚠️ Advertencia menor en Firewall: $_"
          }

      # 3. Instalación Masiva de Herramientas
      - name: 📦 Instalar Herramientas (Core + DB + Utils)
        run: |
          Write-Host "⬇️ Instalando Pack de Ingeniería..."
          choco install rclone winfsp mariadb cloudflared fastfetch vscode -y --no-progress --limit-output

      # 4. Configuración de Base de Datos (VERSIÓN ARRAY)
      - name: 🗄️ Configurar MySQL (MariaDB)
        run: |
          Write-Host "🚀 Iniciando Motor SQL..."
          $service = Get-Service -Name "MariaDB" -ErrorAction SilentlyContinue
          if ($service) { Start-Service "MariaDB" }
          Start-Sleep -Seconds 5
          
          Write-Host "⚙️ Creando DB 'karpux_db'..."
          $sqlQuery = "CREATE DATABASE IF NOT EXISTS karpux_db; ALTER USER 'root'@'localhost' IDENTIFIED BY 'root'; FLUSH PRIVILEGES;"
          $mysqlArgs = @("-u", "root", "-e", $sqlQuery)
          
          try {
              & mysql $mysqlArgs
          }
          catch {
              Write-Host "⚠️ Buscando ruta manual de MySQL..."
              $mysqlPath = Get-ChildItem -Path "C:\Program Files\MariaDB*" -Filter "mysql.exe" -Recurse | Select-Object -First 1 -ExpandProperty FullName
              if (-not $mysqlPath) { throw "❌ No se encontró mysql.exe" }
              & $mysqlPath $mysqlArgs
          }
          Write-Host "✅ DB Lista: user=root, pass=root"

      # 5. Conexión VPN y Montaje de Disco
      - name: ☁️ Conectar Redes (Tailscale + Drive)
        run: |
          # Tailscale
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest.exe" -OutFile "ts.exe"
          Start-Process -FilePath "ts.exe" -ArgumentList "/quiet", "/norestart" -Wait
          & 'C:\Program Files\Tailscale\tailscale.exe' up --authkey=$env:TS_AUTHKEY --hostname=$env:HOSTNAME --unattended --accept-routes
          
          # Rclone
          $configPath = "$env:USERPROFILE\.config\rclone"
          New-Item -ItemType Directory -Force -Path $configPath | Out-Null
          Set-Content -Path "$configPath\rclone.conf" -Value $env:RCLONE_CONF_CONTENT
          
          # Montaje
          Start-Process rclone -ArgumentList "mount gdrive: Z: --volname KarpuxDrive --network-mode --vfs-cache-mode full --vfs-cache-max-size 5G" -WindowStyle Hidden
          Write-Host "✅ Redes Conectadas."

      # 6. Notificación Final
      - name: 🔔 Discord - Servidor Listo
        if: env.DISCORD_WEBHOOK != ''
        run: |
          $ip = & 'C:\Program Files\Tailscale\tailscale.exe' ip -4
          $msg = "🚀 **Karpux Labs ONLINE** `n`n💻 **Host:** $env:HOSTNAME `n📡 **VPN IP:** $ip `n🗄️ **MySQL:** 3306 (root/root) `n`n⚠️ **ACCIÓN REQUERIDA:** Revisa los logs de GitHub para obtener el código de conexión de VS Code Tunnel."
          
          $payload = @{ content = $msg }
          Invoke-RestMethod -Uri $env:DISCORD_WEBHOOK -Method Post -Body ($payload | ConvertTo-Json) -ContentType 'application/json'

      # 7. Loop Principal (CORREGIDO: REFRESCAR PATH)
      - name: 💻 INICIAR VS CODE TUNNEL
        run: |
          # 1. TRUCO DE INGENIERO: Refrescamos el PATH manualmente para que aparezca 'code'
          $env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
          
          # 2. Aseguramos la ruta por si acaso
          if (-not (Get-Command code -ErrorAction SilentlyContinue)) {
            Write-Host "⚠️ 'code' no detectado en PATH global. Añadiendo ruta manual..."
            $env:Path += ";C:\Program Files\Microsoft VS Code\bin"
          }

          # 3. Ejecutamos Fastfetch simple (sin opciones que fallen)
          if (Get-Command fastfetch -ErrorAction SilentlyContinue) { fastfetch }

          Write-Host "============================================="
          Write-Host "      ⚡ KARPUX LABS DEV ENVIRONMENT ⚡      "
          Write-Host "============================================="
          Write-Host "1. Abre en tu navegador: https://github.com/login/device"
          Write-Host "2. Usa el código que aparecerá abajo para autorizar."
          Write-Host "3. Entra a: https://vscode.dev/tunnel/$env:HOSTNAME"
          Write-Host "============================================="
          
          # 4. Ahora sí, iniciamos el túnel sin miedo al éxito
          code tunnel --accept-server-license-terms --name $env:HOSTNAME
